services:
    - name: "svc1"
      listen_port: 80
      balancer: "RoundRobin"

      hosts:
          - "http://localhost/"
          - "http://127.0.0.1"

      upstreams:
          - host: "http://localhost:8000"
            health_uri: "/health" # should start with /
          - host: "http://localhost:9000"
            health_uri: "/" # default value

    - name: "svc2"
      listen_port: 5000
      balancer: "RoundRobin"

      hosts:
          - "http://localhost/"
          - "http://127.0.0.1"

      upstreams:
          - host: "http://localhost:6000"
            health_uri: "/health" # should start with /
          - host: "http://localhost:7000"
            health_uri: "/" # default value
